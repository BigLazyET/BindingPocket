<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:RiveRuntime.Maui.Controls;assembly=RiveRuntime.Maui"
             xmlns:viewModels="clr-namespace:Rive.Demo.ViewModels"
             x:Class="Rive.Demo.Pages.TouchInputPage">
    
    <ContentPage.BindingContext>
        <viewModels:TouchInputViewModel />
    </ContentPage.BindingContext>
    
    <ScrollView>
        <VerticalStackLayout>
            <Label Margin="0,50,0,0"
                   Text="{Binding CurrentStateName, StringFormat='State name: {0}'}"
                   HorizontalOptions="Center"
                   FontAttributes="Bold"
                   FontSize="20" />

            <Label Text="{Binding NumberInputValue, StringFormat='Number input: {0}'}"
                   HorizontalOptions="Center"
                   FontAttributes="Bold"
                   FontSize="20" />

            <controls:RiveSpriteView Margin="0,40,0,0"
                                     ResourceName="lil_guy"
                                     Fit="Contain"
                                     HeightRequest="800"
                                     StateChangedCommand="{Binding StateChangedCommand}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>